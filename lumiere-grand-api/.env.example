PORT=4000
JWT_SECRET=change-this-to-a-long-random-string
CORS_ORIGIN=http://localhost:5173
# Set this to your real backend URL once deployed, e.g. https://api.yourhotel.com
# so uploaded image URLs are always correct regardless of what host receives the request.
BASE_URL=http://localhost:4000
